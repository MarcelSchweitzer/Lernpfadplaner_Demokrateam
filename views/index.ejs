<!DOCTYPE html>
<html>

    <head>
        <head>
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
            <script>
                $(document).ready(()=>{
                    $('#button').click(function() {

                    });
                });
            </script>    
        </head>
    </head>

    <body>
        <button type="button" id="settings">Einstellungen</button>
        <h1>
            Lernpfadplaner 
        </h1>
        <p>
            verfuegbare Module
        </p>
        <ul>
            <% data.learningPathNames.forEach(lp=>{ %>
                <li> 
                    <button type="edit" id="open<%= lp %>" method="POST"><%= lp %></button> 
                    <button type="edit" id="delete<%= lp %>">x</button> 
                </li>
            <% }); %>
        </ul>
        <button type="create" id="createLp">Lernpfad erstellen</button>
    </body>

</html>